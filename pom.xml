<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <!-- Generated by https://start.springboot.io -->
    <!-- 优质的 spring/boot/data/security/cloud 框架中文文档尽在 => https://springdoc.cn -->
    <groupId>com.dyx</groupId>
    <artifactId>SpringAiAlibaba</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <description>SpringAiAlibaba</description>
    <packaging>pom</packaging>
    <name>SpringAIAlibaba-Maven父工程POM配置</name>

    <modules>
        <module>ChatModel01</module>
        <module>ChatClient02</module>
        <module>StreamingOutput03</module>
        <module>PromptTemplate05</module>
        <module>ChatPrompt04</module>
        <module>StructuredOutput06</module>
        <module>Persistent07</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <java.version>21</java.version>
        <spring-boot.version>3.5.14</spring-boot.version>
        <!-- Spring AI（注意：版本号是 1.1.2，不带末尾的 .0）-->
        <spring-ai.version>1.1.2</spring-ai.version>
        <!-- Spring AI Alibaba 1.1.2.0 -->
        <SpringAIAlibaba.version>1.1.2.0</SpringAIAlibaba.version>
        <hutool.version>5.8.22</hutool.version>
    </properties>

    <!-- 统一管理版本号：子模块引依赖时不写 version -->
    <dependencyManagement>
        <dependencies>
            <!--hutool-->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <!-- Spring AI Alibaba Agent Framework -->
            <dependency>
                <groupId>com.alibaba.cloud.ai</groupId>
                <artifactId>spring-ai-alibaba-agent-framework</artifactId>
                <version>${SpringAIAlibaba.version}</version>
            </dependency>
            <!-- DashScope ChatModel 支持（如果使用其他模型，请跳转 Spring AI 文档选择对应的 starter） -->
            <dependency>
                <groupId>com.alibaba.cloud.ai</groupId>
                <artifactId>spring-ai-alibaba-starter-dashscope</artifactId>
                <version>${SpringAIAlibaba.version}</version>
            </dependency>
            <!-- 对话记忆 Redis 持久化 -->
            <dependency>
                <groupId>com.alibaba.cloud.ai</groupId>
                <artifactId>spring-ai-alibaba-starter-memory-redis</artifactId>
                <version>${SpringAIAlibaba.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
